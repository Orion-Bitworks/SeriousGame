using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChangeColor : MonoBehaviour
{
    /*private void Update()
    {
        Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);

        RaycastHit hit;

        if (Physics.Raycast(ray, out hit))
        {
            SelectObject newSelectObject = hit.collider.GetComponent<SelectObject>();

            if (hit.collider.GetComponent<SelectObject>())
            {

                newSelectObject.ChangeColorMethod(newSelectObject.defaultColor);
            }
            else
            {
                newSelectObject.ChangeColorMethod(newSelectObject.newColor);
            }
        }
    }*/
}
